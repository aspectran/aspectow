<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="users">

    <insert id="insertLoginHist" parameterType="club.textchat.user.UserInfo" useGeneratedKeys="true" keyProperty="userNo">
        insert into user_login_hist (username) values (#{username})
    </insert>

</mapper>